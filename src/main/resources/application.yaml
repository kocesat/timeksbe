server:
  port: 8081
  servlet:
    context-path: /timeks
    encoding:
      charset: UTF-8
spring:
  application:
    name: timeksbe
  datasource:
    url: jdbc:postgresql://localhost:5432/timeks
    driver-class-name: org.postgresql.Driver
    username: kocesat
    password: Ankara06
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true